{
  "name": "cosmo-x",
  "version": "0.1.0",
  "description": "X API toolkit — typed client, rate-aware operations, scheduling",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "cosmo-x": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "cli": "node --enable-source-maps dist/cli.js",
    "test": "node --enable-source-maps dist/cli.js test"
  },
  "dependencies": {
    "@xdevplatform/xdk": "^0.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
